# Client Environment Variables
# Copy this file to .env and fill in your values

# API base URL (only needed if NOT using Vite's built-in proxy)
# When running locally with the Vite proxy configured in vite.config.ts, this is not required.
VITE_API_URL=http://localhost:3001
